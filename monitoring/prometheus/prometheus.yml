global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: 'churn-model-monitor'
    static_configs:
      - targets: ['localhost:9100']
